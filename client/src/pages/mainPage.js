import { Box } from "@mui/material";
import StackedBox from "../containers/stackedBox";

const MainPage = () => {
  return (
    <Box>
      <StackedBox />
    </Box>
  );
};

export default MainPage;
